import NavBar from "./UI/NavBar";
import StyledHeader from "./UI/StyledHeader";

const Header = () => {
  return (
    <StyledHeader>
      <NavBar />
    </StyledHeader>
  );
};

export default Header;
